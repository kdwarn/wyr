language: python
python:
  - "3.6"
# command to install dependencies
  - pip3 install -r requirements.txt
# command to run tests
script: python -m pytest --cov=app tests/ -p no:warnings
branches:
  only:
    - master
